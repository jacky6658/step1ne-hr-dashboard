# HR 招募總覽看板 - Web 版

## 📊 功能特色

- **即時數據顯示**：自動每 30 秒更新看板資料
- **Pipeline 視覺化**：各階段候選人分布一目了然
- **提醒系統**：超過 3 天未跟進的候選人自動標示
- **最近動態**：追蹤最新的候選人狀態變更
- **響應式設計**：支援桌面、平板、手機多種裝置

## 🚀 快速啟動

### 開發模式
```bash
cd /Users/user/clawd/projects/hr-dashboard
npm run dev
```

開啟瀏覽器訪問：http://localhost:3000

### 生產模式
```bash
npm run build
npm start
```

## 📁 專案結構

```
hr-dashboard/
├── app/
│   ├── api/
│   │   └── dashboard/
│   │       └── route.ts          # API 路由（讀取/重新生成看板）
│   ├── layout.tsx                # 全局佈局
│   ├── page.tsx                  # 主頁面（儀表板）
│   └── globals.css               # 全局樣式
├── public/                       # 靜態資源
├── package.json
└── README.md
```

## 🔧 技術棧

- **框架**：Next.js 15 (App Router)
- **語言**：TypeScript
- **樣式**：Tailwind CSS
- **圖示**：Lucide React
- **資料來源**：`~/clawd/data/headhunter/dashboard.json`

## 📡 API 端點

### GET /api/dashboard
讀取當前看板資料

**回應範例**：
```json
{
  "generated_at": "2026-02-10T18:45:00",
  "summary": {
    "total_jobs": 5,
    "active_jobs": 3,
    "total_candidates": 12,
    "in_pool": 5,
    "in_process": 4,
    "placed": 2
  },
  "pipeline": { ... },
  "need_followup": 1,
  "recent_activity": [ ... ]
}
```

### POST /api/dashboard
觸發看板重新生成（執行 Python 腳本）

## 🎨 介面說明

### 統計卡片
- **活躍職缺**：目前開放的職缺數量
- **候選人總數**：系統中所有候選人
- **進行中**：正在 Pipeline 中的候選人
- **已成交**：成功到職的人數

### Pipeline 階段
- 📋 履歷池
- 🆕 新進件
- 🔍 匹配中
- 📤 已推薦
- 🎤 面試中
- 💰 Offer中
- ✅ 已報到
- ❌ 已結案

### 提醒區
- 自動顯示超過 3 天未更新的「已推薦」候選人

## 🔄 資料同步

看板會自動從以下來源讀取資料：
- `~/clawd/data/headhunter/dashboard.json`

若需要手動觸發更新：
```bash
python3 ~/clawd/skills/headhunter/scripts/dashboard.py
```

或在 Web 介面點擊「重新整理」按鈕。

## 🌐 部署選項

### 本地網路（推薦）
適合內部團隊使用，安全且快速。

### Vercel（雲端部署）
1. 連結 GitHub 倉庫
2. 設定環境變數（如果需要）
3. 一鍵部署

### Docker
```dockerfile
FROM node:22-alpine
WORKDIR /app
COPY package*.json ./
RUN npm install
COPY . .
RUN npm run build
EXPOSE 3000
CMD ["npm", "start"]
```

## 📝 自定義

### 修改更新頻率
在 `app/page.tsx` 第 75 行：
```typescript
// 預設 30 秒
const interval = setInterval(fetchDashboard, 30000);
```

### 修改主題顏色
在 `app/page.tsx` 修改 gradient 類別：
```typescript
className="min-h-screen bg-gradient-to-br from-slate-900 via-purple-900 to-slate-900"
```

## 🛡️ 注意事項

1. **資料檔案路徑**：確保 `~/clawd/data/headhunter/dashboard.json` 存在
2. **Python 環境**：POST API 需要能執行 Python 3 腳本
3. **權限**：確保 Node.js 有讀取資料檔案的權限

## 🆘 故障排除

### 看板顯示「Dashboard data not found」
→ 執行 `python3 ~/clawd/skills/headhunter/scripts/dashboard.py` 生成資料

### 樣式未正確顯示
→ 確認 `tailwind.config.ts` 設定正確

### API 無法更新
→ 檢查 Python 腳本路徑與權限

---

**版本**：1.0.0  
**建立日期**：2026-02-10  
**開發者**：YuQi 🦞 (AI 助理)
